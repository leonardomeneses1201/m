#pragma once
void registerMetalOreBlocks();      // menas (coal, iron, gold, diamond...)
void registerMetalStorageBlocks();  // bloques de almacenamiento (iron block, etc.)
void registerCopperVariants();      // cobre: oxidado, cortado, encerado
